import React from 'react';

import './errors.scss';

const Errors = () => (
  <section className="error">
    <div>
      Erreur 404, la page n'existe pas
    </div>
  </section>
);
export default Errors;
